from setuptools import find_packages, setup

setup(
    name='lstm',
    packages=find_packages(),
    version='0.1.0',
    description='A repo for the project LSTM.',
    author='ituki shibanuma',
    license='MIT',
)
